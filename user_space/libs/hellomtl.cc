//#include<mtl/mtl.h>

/*
 *      This program demonstrates the use of the MTL (Matrix Template Library)
 */

/*
 * using namespace mtl;
 * using namespace std;
 */
int main(void) {
	/*
	 * const int N=5;
	 * double dx[N]={1,2,3,4,5};
	 * double dy[N]={2,4,8,16,32};
	 * external_vec<double> x(dx,N),y(dy,N);
	 * print_vector(x);
	 * print_vector(y);
	 * double a=x[N-1];
	 * double b=y[N-1];
	 * givens_rotation<double> rot(a,b);
	 * rot.apply(x,y);
	 * print_vector(x);
	 * print_vector(y);
	 */
	return(0);
}
