config_requires = []
dev_requires = []
install_requires = []
build_requires = [
    "pymakehelper",
    "pydmt",
    "pyclassifiers",
    "pylint",
]
test_requires = []
requires = config_requires + install_requires + build_requires + test_requires
