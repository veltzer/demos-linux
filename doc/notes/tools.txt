process statistics:
	sar, sadc(8), sa1(8), sa2(8), sadf(1), isag(1).
	mpstat
	vmstat
	dstat
	pidstat
	top, htop
	/proc, /sys, /debugfs filesystems
	ps pstree pgrep
	skill, pkill, kill, killall, killall5
	nice, renice, snice
	atop, atopsar
	saidar
	nmon

memory:
	free
	smem

system:
	uptime

locking and deadlocks:
	mutrace (for lock contention)

io:
	iostat
	iotop, ionice
	hdparm, sdparm, lsscsi (/proc/scsi/scsi)
	blktool

cpu:
	taskset
	cpulimit
	schedtop (doesn't have a package as of yet).
	schedtool

process ids:
	pidof
	ptrace
	strace
	ltrace
	pstack

used files:
	fuser, lsof

debugggers:
	valgrind

profilers:
	valgrind
	google-perftools
	google-pprof

core dumps:
	cgdb
	eclipse
	xxgdb
	qtcreator
	gdb
	gdbtui
	gcore
	resolve_stack_dump
	dwarves

kernel:
	crash
	slabtop
	kerneltop
	btrace
	kexec

networking:
	ss (socket statistcs including queue state)
	ping/ping6
	traceroute/traceroute6
	jnettop
	iftop
	iptraf
	ngrep
	tcpdump
	wireshark
	dumpcap
	rawshark
	tshark
	editcap
	mergecap
	text2pcap
	ethtool
	netstat
	nc
	route
	ifconfig
	iwconfig
	iwlist
	telnet
	brctl (bridging utilities)
	vnstat
	vnstati
	netperf
	iptables
	iperf

java debugging:
	jstack
	jdb

programming tracing:
	mtrace (muntrace,malloc tracing).
	dmalloc(1)
	pthread_trace_* (pthread tracing).
	electric-fence

power management:
	powertop
	turbostat

general tools (not intended for just one specific utility):
	systemtap
	perf
	oprofile
	sysprof
	sysdig

pipe performance:
	pipemeter: cat /dev/zero | pipemeter | wc -c
	pv

watchdogs/fault helpers:
	catchsegv: part of libc.
		run like this: catchsegv [progname] [args...]
		if your program faults then it will print a stack trace + registers + position + program map.

mysql:
	mytop
	sysbench

programming:
	cdecl
	lint (various sorts)

elf tools:
	nm
	ldd
	pldd
	ld.so
	objdump
	objcopy
	size
	readelf
	chrpath
	patchelf

database admin:
	tora
	toad
	mysql
	sqlite3
	psql

realtime:
	chrt
	hwlatdetect
	latencytop
	cyclictest (hackbench, ... many other here from rt-tests)

listing stuff:
	lshal - list HAL devices
	lsscsi - list scsi devices
	lspci - list pci devices
	lsusb - list usb devices
	lshw - list hardware for the current machine
	lscpu - list cpu info for the current machine
	lsmod - list currently loaded kernel modules
	lsblk - list block devices on the current machine
	lspcmia - list pcmcia devices
	nproc - show number of processing units available
	x86info - show hardware info on x86.
	dmidecode - DMI table decoder

gui:
	icinga (web app)
	nagios (web app)
	ksysguard (GUI app)
	gnome-system-montor (GUI app)
	xfce4-taskmanager (GUI app)
	linux process explorer (procexp) - no package for ubuntu (GUI app)
	mrtg (GUI app)
	monit (??)
	munin (??)
