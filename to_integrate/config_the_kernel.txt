


How can we configure the kernel? (aside from recompiling)

- boot parameters (this needs to be set at the bootloader)

- load and unload modules (and set module parameters)

- /proc, /sys and other virtual file systems

- system calls
	
- sysctl (/etc/sysctl.conf, /etc/sysctl.conf.d/*, ...)

- command line tools
