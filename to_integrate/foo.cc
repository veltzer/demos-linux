


void calculate_blabla(int* array, int count) {
#ifdef DEBUG_CALCULATE
	if(array==NULL) {
		// throw exception or other error behaviour...
	}
#endif // DEBUG_CALCULATE
}


void foo() {
#ifdef DEBUG_CALCULATE
	if(array==NULL) {
		// throw exception or other error behaviour...
	}
#endif // DEBUG_CALCULATE
}
